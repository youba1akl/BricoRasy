import 'package:flutter/material.dart';

class AddAnnoScreen extends StatelessWidget {
  const AddAnnoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Add Annonces', style: TextStyle(fontSize: 50)),
    );
  }
}